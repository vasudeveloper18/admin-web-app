# MongoDB (required in production)
MONGODB_URI=mongodb://127.0.0.1:27017/admin-web-app

# JWT (set strong secrets in production — Render env)
JWT_ACCESS_SECRET=change_me_access
JWT_REFRESH_SECRET=change_me_refresh

PORT=3001
